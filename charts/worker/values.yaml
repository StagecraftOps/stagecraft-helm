namespace: stagecraft
nameOverride: ""

image:
  repository: yourregistry/stagecraft-worker
  tag: latest
  pullPolicy: Always

service:
  enabled: false

serviceAccount:
  create: true
  roleArn: ""

# Populates the stagecraft-worker-secrets Kubernetes Secret from AWS Secrets
# Manager via the External Secrets Operator (see the umbrella chart's
# clustersecretstore.yaml). Shared by both the worker and consumer Deployments
# below — they use the same ServiceAccount and the same secret name.
externalSecret:
  enabled: true
  storeName: stagecraft
  awsSecretName: stagecraft-worker-secrets
  refreshInterval: 1h

env:
  ENVIRONMENT: production
  AWS_REGION: us-east-1
  BEDROCK_MODEL_ID: anthropic.claude-sonnet-4-6
  USE_MULTI_AGENT: "true"
  USE_MCP_TOOLS: "true"
  FRONTEND_URL: https://stagecraft.example.com

# The Celery worker process — picks up tasks from Redis, runs the agent fleet.
worker:
  replicaCount: 2
  command: []
  containerPort: 8080
  livenessProbe:
    httpGet:
      path: /healthz
      port: 8080
    initialDelaySeconds: 10
    periodSeconds: 15
  readinessProbe:
    httpGet:
      path: /ready
      port: 8080
    initialDelaySeconds: 5
    periodSeconds: 10
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi

# The SQS -> Celery bridge process — same image, different entrypoint.
consumer:
  replicaCount: 1
  command: ["python", "-m", "app.sqs_consumer"]
  containerPort: 8080
  livenessProbe:
    httpGet:
      path: /healthz
      port: 8080
    initialDelaySeconds: 10
    periodSeconds: 15
  readinessProbe:
    httpGet:
      path: /ready
      port: 8080
    initialDelaySeconds: 5
    periodSeconds: 10
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
